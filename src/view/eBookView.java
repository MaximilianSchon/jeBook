package view;

import javafx.scene.web.WebView;

public class eBookView {
    private WebView webView;

    public eBookView() {
        this.webView = new WebView();
    }
    /**
     * TODO
     * Create a basic eBook View that displays one "page" at a time
     * Tie the URLs in the WebView to actual pages
     * Load CSS (if I want?)
     * Create highlighting
     * Create commenting
     * Maybe handle some of the parsing in the model (ebook) instead.
     */
}
